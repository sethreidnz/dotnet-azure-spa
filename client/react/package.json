{
  "name": "dotnet-azuread-client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^15.5.4",
    "react-dom": "^15.5.4"
  },
  "devDependencies": {
    "ncp": "^2.0.0",
    "react-scripts": "0.9.5",
    "rimraf": "^2.6.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "clean-wwwroot": "rimraf ../../server/wwwroot",
    "copy-to-wwwroot": "npm run clean-wwwroot && ncp build ../../server/wwwroot/"
  }
}
